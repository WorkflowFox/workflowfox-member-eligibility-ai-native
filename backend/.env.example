# SQLAlchemy database URL. Local development defaults to a SQLite file in
# this directory if unset. A future PostgreSQL URL (e.g.
# postgresql+psycopg://user:pass@host:5432/dbname) can be used here without
# any change to application/business logic.
DATABASE_URL=sqlite:///./member_eligibility.db
