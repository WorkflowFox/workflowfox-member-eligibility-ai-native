# Base URL of the FastAPI eligibility backend (default http://localhost:8092).
VITE_API_BASE_URL=http://localhost:8092

# Simulated eligibility-service latency in milliseconds, used only by
# MockEligibilityService in tests/isolated frontend development (default 900).
VITE_MOCK_LATENCY_MS=900
